# Core runtime dependencies
httpx==0.28.1
playwright==1.50.0
rich==14.0.0

# Optional — stealth mode for browser automation
playwright-stealth==1.0.6

# Optional — WASM PoW solver (Layer 1)
wasmtime==28.0.0

# Optional — API server
fastapi==0.128.8
uvicorn==0.40.0
python-multipart==0.0.20

# Development / testing
pytest==8.3.5
pytest-asyncio==0.25.3
